#ifndef MIRATH_DATA_TYPE_ARITH_H
#define MIRATH_DATA_TYPE_ARITH_H

#include <inttypes.h>

/// we represent a single F2 element (1 bit) in a single byte.
typedef uint8_t ff_t;

/// we represent each field element with a single byte.
typedef uint8_t ff_mu_t;

#endif //MIRATH_DATA_TYPE_ARITH_H
